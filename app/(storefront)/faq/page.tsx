import FaqShell from '@/components/faq-shell';

export const metadata = {
  title: 'FAQ | AuraFume',
  description:
    'Answers to common questions about orders, payments, shipping, returns, products, and your AuraFume account.',
};

export default function FaqPage() {
  return <FaqShell />;
}
